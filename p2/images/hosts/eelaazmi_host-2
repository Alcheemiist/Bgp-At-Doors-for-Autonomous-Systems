FROM alpine:3.17.1

# Install necessary networking tools
RUN apk add --no-cache iproute2

# Copy the startup script
COPY ./setup_second_host.sh /setup_second_host.sh
RUN chmod +x /setup_second_host.sh

# Set the startup script as the entrypoint
ENTRYPOINT ["/setup_second_host.sh"]